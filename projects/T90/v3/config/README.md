# V3 Config

This directory is reserved for V3 configuration files.

Recommended future contents:

- experiment configuration
- feature selection policy
- label definition policy
- runtime interface defaults

Do not copy old root-level config files here unless they are explicitly revalidated for the V3 objective.
